# ===========================================================================
# Raw data -- regenerated by the scripts, and the assignment asks for "no data
# files" in the repo.  This covers the FRED/Yahoo market panel and every GDELT
# cache written by the collector (war_news_volume.csv, _vol_*.csv, _hl_*.csv,
# headlines.csv, day_flags.csv, day_regimes.csv).
# ===========================================================================
data/

# ===========================================================================
# Course reading -- the two reference papers and their extracted text.  They
# are on Brightspace; no need (or right) to republish them in the repo.
# ===========================================================================
Effects_of_War_Risk.pdf
Effects_of_War_Risk.txt
Identification_through_Heteroskedasticity.pdf
Identification_through_Heteroskedasticity.txt

# ===========================================================================
# Python
# ===========================================================================
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ===========================================================================
# Editor / OS
# ===========================================================================
.vscode/
.idea/
.DS_Store
Thumbs.db

# ===========================================================================
# Secrets (e.g. FRED_API_KEY); the pipeline runs without one.
# ===========================================================================
.env

# NOTE: outputs/ IS committed on purpose -- those small tables
# (table1_war_news_days.csv, table2_sensitivity*.csv, table3_variance*.csv)
# are the report's deliverables.  uv.lock and .python-version are committed too,
# so `uv sync` reproduces the exact environment.
